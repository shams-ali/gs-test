import React from 'react';
import Flow from '../flow/flow';
import data from '../../data';

const Assessment = () => (
  <div>
    <h1>Assessment</h1>
      <Flow data={data} />
    </div>
);

export default Assessment;